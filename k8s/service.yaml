apiVersion: v1
kind: Service
metadata:
  name: student-api
  namespace: cqrs-lab
spec:
  type: NodePort
  selector:
    app: student-api
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30082
